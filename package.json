{
  "name": "nederlandse-fietsknooppunten-tracker",
  "version": "1.0.0",
  "description": "Track your Dutch cycling node routes with real OpenStreetMap data",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "npm run build && nodemon dist/server.js",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "download": "npm run build && node dist/scripts/download-all-nodes.js nodes routes",
    "download:nodes": "npm run build && node dist/scripts/download-all-nodes.js nodes",
    "download:routes": "npm run build && node dist/scripts/download-all-nodes.js routes",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "docker:build": "docker build -t fietsrouteapp .",
    "docker:run": "docker run -p 3000:3000 fietsrouteapp",
    "docker:compose": "docker-compose up --build",
    "docker:compose:prod": "docker-compose up -d --build"
  },
  "keywords": [
    "cycling",
    "netherlands",
    "openstreetmap",
    "fietsknooppunten",
    "routes",
    "tracking"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.2",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/axios": "^0.9.36",
    "@types/cors": "^2.8.19",
    "@types/express": "^4.17.21",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "@types/supertest": "^6.0.3",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^7.1.4",
    "ts-jest": "^29.4.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
